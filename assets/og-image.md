# Open Graph image placeholder

Use `assets/og-image.png` for social previews.

Recommended dimensions:
- 1200 x 630 px (best for Open Graph + Twitter large card)
- Keep critical text centered in a safe area
- File size target: under 300 KB for fast sharing previews
